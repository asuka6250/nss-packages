--- a/mc_netlink.c
+++ b/mc_netlink.c
@@ -776,7 +776,7 @@ static void __mc_netlink_receive(struct
 					mc->local_query_interval = i->val * HZ;
 					if (timer_pending(&mc->qtimer) ?
 						time_after(mc->qtimer.expires, jiffies + mc->local_query_interval) :
-							try_to_del_timer_sync(&mc->qtimer) >= 0) {
+							timer_delete_sync_try(&mc->qtimer) >= 0) {
 						if (mc->started)
 							mod_timer(&mc->qtimer, jiffies + mc->local_query_interval);
 					}
--- a/mc_wifi_events.c
+++ b/mc_wifi_events.c
@@ -24,6 +24,7 @@
 #include "mc_netlink.h"
 #include "mc_snooping.h"
 #include "mc_forward.h"
+#include "mc_wifi_events.h"
 #include <linux/in6.h>
 
 struct sta_leave_event {
@@ -81,7 +82,7 @@ static int mc_event_recv(struct socket *
  * mc_sta_leave_event()
  *	Delete the mac entry of sta from bridge snooping table
  */
-void mc_sta_leave_event(struct net_device *dev,__u8 *from_mac)
+static void mc_sta_leave_event(struct net_device *dev,__u8 *from_mac)
 {
 
 	struct mc_struct *mc;
@@ -154,7 +155,7 @@ void mc_sta_leave_event(struct net_devic
  * mc_process_sta_leave_event()
  * 	Parse and process sta_leave event
  */
-void mc_process_sta_leave_event(struct nlmsghdr *nlh, int cmd)
+static void mc_process_sta_leave_event(struct nlmsghdr *nlh, int cmd)
 {
 	struct nlattr **tb = NULL;
 	uint8_t mac[ETH_ALEN] = {0};
@@ -438,7 +439,7 @@ void mc_handle_vbss_event(struct net_dev
 /* mc_process_vendor_event()
  *	Process the vendor events
  */
-void mc_process_vendor_event(struct nlmsghdr *nlh, int cmd)
+static void mc_process_vendor_event(struct nlmsghdr *nlh, int cmd)
 {
 	struct net_device *dev;
 	u_int32_t ifidx;
@@ -557,7 +558,7 @@ static int mc_event_handler(void *buf, i
  * 	Parse the generic control family response message and return
  * 	the multicast id of MLME and VENDOR multicast groups of nl80211 family.
  */
-int mc_get_event_id(struct nlmsghdr *nlh, const char *event_name, int *event_id)
+static int mc_get_event_id(struct nlmsghdr *nlh, const char *event_name, int *event_id)
 {
     struct nlattr *tb[CTRL_ATTR_MAX+1];
     struct nlattr *mcgrp;
@@ -608,7 +609,7 @@ int mc_get_event_id(struct nlmsghdr *nlh
  *	Get the multicast id of MLME and VENDOR multicast groups of nl80211 family.
  */
 
-int mc_parse_genl_ctrl_response(struct nlmsghdr *nlh, int *mlme_id, int *vendor_id)
+static int mc_parse_genl_ctrl_response(struct nlmsghdr *nlh, int *mlme_id, int *vendor_id)
 {
 	int res_mlme, res_vendor;
 
@@ -642,7 +643,7 @@ int mc_parse_genl_ctrl_response(struct n
  *	Construct a message to generic control family to resolve the
  *	nl80211 multicast groups.
  */
-struct nlmsghdr *mc_construct_nl_message(void)
+static struct nlmsghdr *mc_construct_nl_message(void)
 {
 	int flags = GFP_ATOMIC;
 	struct sk_buff *skb;
@@ -705,7 +706,7 @@ err:
  *	Resolve the nl80211 family multicast groups to
  *	receive Wi-Fi specific events.
  */
-int mc_resolve_nl80211_family(struct socket *sock, struct sockaddr_nl *addr)
+static int mc_resolve_nl80211_family(struct socket *sock, struct sockaddr_nl *addr)
 {
 	struct nlmsghdr *nlh;
 	struct kvec iov = {0};
--- a/mc_snooping.c
+++ b/mc_snooping.c
@@ -649,7 +649,7 @@ static void mc_mdb_destroy(struct mc_mdb
 	}
 
 	hlist_del_rcu(&mdb->hlist);
-	del_timer_sync(&mdb->etimer);
+	timer_delete_sync(&mdb->etimer);
 
 	if (mdb->flood_ifcnt || mdb->encap_dev_cnt) {
 		mdb->flood_ifcnt = 0;
@@ -681,7 +681,7 @@ static void mc_agingtimer_reset(struct m
 
 	if (timer_pending(&mc->agingtimer) ?
 			time_after(mc->agingtimer.expires, jiffies + expires) :
-				try_to_del_timer_sync(&mc->agingtimer) >= 0) {
+				timer_delete_sync_try(&mc->agingtimer) >= 0) {
 		mod_timer(&mc->agingtimer, jiffies + expires);
 		MC_PRINT("Reset Group Membership Interval ageing timer, expires = %u\n",
 				jiffies_to_msecs(expires) / 1000);
@@ -709,7 +709,7 @@ static void mc_rtimer_reset(struct mc_st
 
 	if (timer_pending(&mc->rtimer) ?
 			time_after(mc->rtimer.expires, jiffies + expires) :
-				try_to_del_timer_sync(&mc->rtimer) >= 0) {
+				timer_delete_sync_try(&mc->rtimer) >= 0) {
 		mod_timer(&mc->rtimer, jiffies + expires);
 		MC_PRINT("Reset Querier Interval ageing timer, expires = %u\n",
 				jiffies_to_msecs(expires) / 1000);
@@ -726,7 +726,7 @@ static void mc_mdb_expired(unsigned long
 #endif
 {
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0))
-	struct mc_mdb_entry *mdb = from_timer(mdb, tl, etimer);
+	struct mc_mdb_entry *mdb = container_of(tl, typeof(*mdb), etimer);
 #else
 	struct mc_mdb_entry *mdb = (struct mc_mdb_entry *)data;
 #endif
@@ -1219,7 +1219,7 @@ static void mc_leave_group(struct mc_str
 	fg->ageing_timer = now - expire_time + expire_delay;
 	if (timer_pending(&mc->agingtimer) ?
 		time_after(mc->agingtimer.expires, jiffies + expire_delay) :
-		try_to_del_timer_sync(&mc->agingtimer) >= 0) {
+		timer_delete_sync_try(&mc->agingtimer) >= 0) {
 		mod_timer(&mc->agingtimer, jiffies + expire_delay);
 		MC_PRINT("Reset Group Membership Interval ageing timer, expires = %u ms\n",
 			leave_delay_msecond);
@@ -2086,7 +2086,7 @@ static void mc_query_cycle_start(struct
 			goto out;
 		if (timer_pending(&mdb->etimer) ?
 				time_after(mdb->etimer.expires, now + root_qe->max_resp_time) :
-				try_to_del_timer_sync(&mdb->etimer) >= 0) {
+				timer_delete_sync_try(&mdb->etimer) >= 0) {
 			mdb->timer_base = now;
 			mod_timer(&mdb->etimer, now + root_qe->max_resp_time);
 			mc_query_timer_dump(mdb, root_qe->max_resp_time, 1);
@@ -2101,7 +2101,7 @@ static void mc_query_cycle_start(struct
 					continue;
 				if (timer_pending(&mdb->etimer) ?
 						time_after(mdb->etimer.expires, now + root_qe->max_resp_time) :
-						try_to_del_timer_sync(&mdb->etimer) >= 0) {
+						timer_delete_sync_try(&mdb->etimer) >= 0) {
 					mdb->timer_base = now;
 					mod_timer(&mdb->etimer, now + root_qe->max_resp_time);
 					mc_query_timer_dump(mdb, root_qe->max_resp_time, 0);
@@ -2952,10 +2952,10 @@ static int mc_stop(struct mc_struct *mc)
 	mc_mdb_flush(mc);
 	mc_rlist_flush(mc);
 
-	del_timer_sync(&mc->qtimer);
-	del_timer_sync(&mc->agingtimer);
-	del_timer_sync(&mc->rtimer);
-	del_timer_sync(&mc->evtimer);
+	timer_delete_sync(&mc->qtimer);
+	timer_delete_sync(&mc->agingtimer);
+	timer_delete_sync(&mc->rtimer);
+	timer_delete_sync(&mc->evtimer);
 
 	return 0;
 }
@@ -2981,7 +2981,7 @@ static void mc_router_cleanup(unsigned l
 {
 	int delay_reset = 0;
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0))
-	struct mc_struct *mc = from_timer(mc, tl, rtimer);
+	struct mc_struct *mc = container_of(tl, typeof(*mc), rtimer);
 #else
 	struct mc_struct *mc = (struct mc_struct *)data;
 #endif
@@ -3036,7 +3036,7 @@ static void mc_mdb_query(unsigned long d
 {
 	int i;
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0))
-	struct mc_struct *mc = from_timer(mc, tl, qtimer);
+	struct mc_struct *mc = container_of(tl, typeof(*mc), qtimer);
 #else
 	struct mc_struct *mc = (struct mc_struct *)data;
 #endif
@@ -3091,7 +3091,7 @@ static void mc_mdb_cleanup(unsigned long
 {
 	int i;
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0))
-	struct mc_struct *mc = from_timer(mc, tl, agingtimer);
+	struct mc_struct *mc = container_of(tl, typeof(*mc), agingtimer);
 #else
 	struct mc_struct *mc = (struct mc_struct *)data;
 #endif
@@ -3268,7 +3268,7 @@ static void mc_event_delay(unsigned long
 #endif
 {
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0))
-	struct mc_struct *mc = from_timer(mc, tl, evtimer);
+	struct mc_struct *mc = container_of(tl, typeof(*mc), evtimer);
 
 	mc_netlink_event_send(mc, MC_EVENT_MDB_UPDATED,	0, NULL);
 #else
