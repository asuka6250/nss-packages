--- a/nss_init.c
+++ b/nss_init.c
@@ -1034,5 +1034,6 @@ module_init(nss_init);
 module_exit(nss_cleanup);
 
 MODULE_DESCRIPTION("QCA NSS Driver");
+MODULE_INFO(description, "QCA NSS Driver");
 MODULE_AUTHOR("Qualcomm Atheros Inc");
 MODULE_LICENSE("Dual BSD/GPL");
--- a/nss_core.c
+++ b/nss_core.c
@@ -56,7 +56,7 @@
  * following kernel versions. Before enabling the driver in new kernels,
  * the skb recycle code must be checked against Linux skb handling.
  *
- * Tested on: 3.4, 3.10, 3.14, 3.18, 4.4, 5.4 and 6.6
+ * Tested on: 3.4, 3.10, 3.14, 3.18, 4.4, 5.4, 6.6 and 6.18
  */
 #if (!( \
 (((LINUX_VERSION_CODE >= KERNEL_VERSION(3, 4, 0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(3, 5, 0)))) || \
@@ -65,7 +65,8 @@
 (((LINUX_VERSION_CODE >= KERNEL_VERSION(3, 18, 0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(3, 19, 0)))) || \
 (((LINUX_VERSION_CODE >= KERNEL_VERSION(4, 4, 0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(4, 5, 0)))) || \
 (((LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(5, 5, 0)))) || \
-(((LINUX_VERSION_CODE >= KERNEL_VERSION(6, 6, 0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 7, 0))))))
+(((LINUX_VERSION_CODE >= KERNEL_VERSION(6, 6, 0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 7, 0)))) || \
+(((LINUX_VERSION_CODE >= KERNEL_VERSION(6, 18, 0)) && (LINUX_VERSION_CODE < KERNEL_VERSION(6, 19, 0))))))
 #error "Check skb recycle code in this file to match Linux version"
 #endif
 
--- a/nss_crypto_cmn_stats.c
+++ b/nss_crypto_cmn_stats.c
@@ -17,6 +17,8 @@
  **************************************************************************
  */
 
+#include <linux/vmalloc.h>
+
 #include "nss_core.h"
 #include "nss_crypto_cmn_stats.h"
 #include "nss_crypto_cmn_strings.h"
--- a/nss_dma_stats.c
+++ b/nss_dma_stats.c
@@ -17,6 +17,8 @@
  **************************************************************************
  */
 
+#include <linux/vmalloc.h>
+
 #include "nss_core.h"
 #include "nss_dma.h"
 #include "nss_dma_stats.h"
--- a/nss_dtls_cmn_stats.c
+++ b/nss_dtls_cmn_stats.c
@@ -17,6 +17,8 @@
  ***************************************************************************
  */
 
+#include <linux/vmalloc.h>
+
 #include "nss_core.h"
 #include "nss_dtls_cmn.h"
 #include "nss_dtls_cmn_stats.h"
--- a/nss_ipsec_cmn_stats.c
+++ b/nss_ipsec_cmn_stats.c
@@ -17,6 +17,8 @@
  ***************************************************************************
  */
 
+#include <linux/vmalloc.h>
+
 #include "nss_core.h"
 #include "nss_ipsec_cmn.h"
 #include "nss_ipsec_cmn_stats.h"
--- a/nss_mirror_stats.c
+++ b/nss_mirror_stats.c
@@ -17,6 +17,8 @@
  **************************************************************************
  */
 
+#include <linux/vmalloc.h>
+
 #include "nss_stats.h"
 #include "nss_core.h"
 #include "nss_mirror.h"
--- a/nss_ppe_vp.c
+++ b/nss_ppe_vp.c
@@ -20,6 +20,8 @@
  * Header file for qca-ssdk APIs
  */
 #include <ref/ref_vsi.h>
+#include <linux/vmalloc.h>
+
 
 #include "nss_ppe_vp.h"
 #include "nss_ppe_vp_stats.h"
--- a/nss_qvpn_stats.c
+++ b/nss_qvpn_stats.c
@@ -17,6 +17,8 @@
  **************************************************************************
  */
 
+#include <linux/vmalloc.h>
+
 #include "nss_core.h"
 #include <nss_qvpn.h>
 #include "nss_qvpn_stats.h"
--- a/nss_tls_stats.c
+++ b/nss_tls_stats.c
@@ -17,6 +17,8 @@
  ******************************************************************************
  */
 
+#include <linux/vmalloc.h>
+
 #include "nss_core.h"
 #include "nss_tls.h"
 #include "nss_tls_stats.h"
--- a/nss_c2c_tx.c
+++ b/nss_c2c_tx.c
@@ -330,35 +330,7 @@ static struct ctl_table nss_c2c_tx_table
 		.maxlen		= sizeof(int),
 		.mode		= 0644,
 		.proc_handler	= &nss_c2c_tx_performance_test_handler,
-	},
-	{ }
-};
-
-static struct ctl_table nss_c2c_tx_dir[] = {
-	{
-		.procname		= "c2c_tx",
-		.mode			= 0555,
-		.child			= nss_c2c_tx_table,
-	},
-	{ }
-};
-
-static struct ctl_table nss_c2c_tx_root_dir[] = {
-	{
-		.procname		= "nss",
-		.mode			= 0555,
-		.child			= nss_c2c_tx_dir,
-	},
-	{ }
-};
-
-static struct ctl_table nss_c2c_tx_root[] = {
-	{
-		.procname		= "dev",
-		.mode			= 0555,
-		.child			= nss_c2c_tx_root_dir,
-	},
-	{ }
+	}
 };
 
 static struct ctl_table_header *nss_c2c_tx_header;
@@ -378,7 +350,7 @@ void nss_c2c_tx_register_sysctl(void)
 	/*
 	 * Register sysctl table.
 	 */
-	nss_c2c_tx_header = register_sysctl_table(nss_c2c_tx_root);
+	nss_c2c_tx_header = register_sysctl("dev/nss/c2c_tx", nss_c2c_tx_table);
 }
 
 /*
